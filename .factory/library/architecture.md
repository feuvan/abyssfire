# Architecture

Architectural decisions, patterns, and conventions discovered during the mission.

**What belongs here:** Architectural patterns, system interactions, code organization decisions, performance patterns.

---

## Scene Architecture

- `BootScene` → `MenuScene` → `ZoneScene` + `UIScene` (parallel overlay)
- ZoneScene is the main game loop; UIScene is an overlay scene
- Zone transitions restart ZoneScene with playerStats data object

## System Lifecycle

**Per-zone (recreated on zone change):**
- CombatSystem, SkillEffectSystem, LightingSystem, VFXManager, WeatherSystem, TrailRenderer, PathfindingSystem

**Persistent (survive zone transitions):**
- InventorySystem, QuestSystem, HomesteadSystem, AchievementSystem, SaveSystem

**Singletons:**
- audioManager (module-level instance)

## Communication Patterns

- **EventBus**: Central pub/sub with typed GameEvents enum (~30+ events)
- **Direct references**: ZoneScene holds refs to all systems
- **UI refresh**: EventBus `'ui:refresh'` on zone transitions

## Procedural Art Pipeline

1. `SpriteGenerator.ts` drawer registry: each entity type has a drawer function
2. Textures generated on-demand (not all at boot)
3. `DrawUtils.ts` provides shared utilities: `softOutline`, `rimLight`, `applyDirectionalLighting`
4. `CharacterAnimator.ts` handles animation states with hit freeze, body decomposition on death
5. Tile transitions generated by `generateTransitionTile()` with noise-displaced boundaries

## Map System

- All 5 zones use `MapGenerator.generate()` for procedural tile generation
- Map data defines anchor points (camps, exits, spawns); terrain is procedural
- Viewport culling renders only visible tiles (~300-500 at a time)
- Tile types: 0=grass, 1=dirt, 2=stone, 3=water, 4=wall, 5=camp

## Performance Patterns

- Viewport culling for tiles (100ms throttle)
- Cached equipment stats (invalidated on equip change)
- Throttled quest/NPC marker checks (500ms)
- World position pre-computation on zone load
- BGM lazy-loading per zone

commands:
  install: npm install
  typecheck: npx tsc --noEmit
  build: npm run build
  test: npx vitest run
  lint: echo "no linter configured"

services:
  web:
    start: cd /Users/feuvan/src/abyssfire && PORT=5173 npm run dev
    stop: lsof -ti :5173 | xargs kill 2>/dev/null || true
    healthcheck: curl -sf http://localhost:5173/abyssfire/
    port: 5173
    depends_on: []
